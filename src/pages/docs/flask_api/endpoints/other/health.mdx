---
title: "Health check"
description: "GET - Health check"
---
```
GET {{NEXT_PUBLIC_API_BASE_URL}}/api/health
```
# Health check
If the Flask service is running, return the status 'healthy' as a JSON. This API route is only
used during local development. 